import React from 'react';
import { NextPage } from 'next';
import { Layout, CartPage } from 'components';

const Cart: NextPage = () => (
  <Layout>
    <CartPage/>
  </Layout>
);

export default Cart;
