import React from 'react';
import { NextPage } from 'next';
import { HomePage, Layout } from 'components';

const Name: NextPage = () => (
  <Layout>
    <HomePage/>
  </Layout>
);

export default Name;
